<%= render 'navigation' %>

<h1>Show Project</h1>

  <div class="field">
    <%= label_tag 'Name' %>
    <%= label_tag @project.name %>
  </div>

  <div class="field">
    <%= label_tag 'Description' %>
    <%= label_tag @project.description %>
  </div>


  <div class="field">
    <%= label_tag 'Display?' %>
    <%= label_tag (@project.display) ? 'Yes': 'No' %>
  </div>

  <div class="actions">
    <%= link_to 'Edit', edit_admin_project_path(@project), :class => 'button' %>
    <%= link_to 'Back', admin_projects_path, :class => 'button' %>
  </div>
